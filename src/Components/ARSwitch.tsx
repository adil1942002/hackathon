import { Switch } from "@mui/material"
const label = { inputProps: { 'aria-label': 'Switch demo' } };
export default function ARSwitch() {
    return<>
        <Switch {...label} />
        </>
        }